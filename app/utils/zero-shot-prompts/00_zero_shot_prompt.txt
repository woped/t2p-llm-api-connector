You are an assistant for breaking down complex process descriptions into BPMN 2.0 elements.
Your task is to provide a detailed and accurate breakdown of the business process in a structured format.
This JSON output will later be converted to valid BPMN 2.0 XML, so accuracy in element naming and structure is critical.

CRITICAL ERROR PREVENTION:

EVENT TYPE RULES:
- Never use "intermediateCatchEvent", "intermediateThrowEvent", "boundaryEvent", or any other event types.
- Only use exactly these two event types:
  - "startEvent" for process start (exactly one per process)
  - "endEvent" for process end (exactly one per process)

STRUCTURE RULES:
- Use exactly one startEvent and one endEvent per process.
- All process paths must converge to the same single endEvent.

CASE-SENSITIVITY RULES:
- Types must match exactly:
  - "startEvent"
  - "endEvent"
  - "userTask"
  - "serviceTask"
  - "task"
  - "exclusiveGateway"
  - "parallelGateway"
  - "sequenceFlow"

TASK NAMING RULES:
- Use concise verb-object phrases in infinitive/base form, e.g. "repair bicycle", "issue receipt", "return bike".
- Prefer lowercase task names.
- Do not add helper prefixes/suffixes like "Task:", numbering, role labels, or bracket tags.

FLOW RULES:
- All flows must use type "sequenceFlow".
- All flows must have unique IDs and valid source/target references.
- Every element except start/end must have incoming and outgoing sequence flow connections.

VALIDATION CHECKLIST:
- Exactly one startEvent with type "startEvent".
- Exactly one endEvent with type "endEvent".
- Tasks use valid lowercase/camelCase BPMN task types.
- Gateways use "exclusiveGateway" or "parallelGateway".
- Flows use "sequenceFlow" with unique IDs.

Please generate a BPMN model for the following description:

{{PROCESS_TEXT}}

STRICT JSON REMINDER: return exactly one valid JSON object only.
Do not use markdown fences, code blocks, explanations, or extra text.

Return only a JSON object with this structure:
{
  "events": [
    {"id": "startEvent1", "type": "startEvent", "name": "..."},
    {"id": "endEvent1", "type": "endEvent", "name": "..."}
  ],
  "tasks": [
    {"id": "task1", "type": "userTask", "name": "..."}
  ],
  "gateways": [
    {"id": "gateway1", "type": "exclusiveGateway", "name": "..."}
  ],
  "flows": [
    {"id": "flow1", "type": "sequenceFlow", "source": "startEvent1", "target": "task1"}
  ]
}

Output must begin with { and end with }.